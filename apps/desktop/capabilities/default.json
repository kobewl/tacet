{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tacet 各窗口的默认权限：Tauri 核心能力（事件监听、窗口操作），外加把窗口拖着走所需的拖动权限。",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
